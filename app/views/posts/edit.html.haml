= form_for @posts, :url => {:action => :update} do |f|
  %h3 投稿する
  = f.label "画像をアップロード"
  = f.file_field :image
  = f.label 'text'
  = f.text_area :text
  -# = f.hidden_field :user_id, value: current_user.id
  = f.submit "send"