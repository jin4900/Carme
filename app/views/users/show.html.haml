.contents.row
  %p
    = @nickname
    さんの投稿一覧
  - @posts.each do |post|
    .content_post{:style => "background-image: url(#{post.image});"}
      = simple_format(post.text)
      %span.name= post.name
