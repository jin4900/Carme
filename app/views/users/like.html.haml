.wrapper
  = render 'shared/header'
.content
  .user
    .nickname
      = @nickname
      さんのお気に入り
    %ul.photo-list
      - @post.each do |post|
        %li
          = link_to "/posts/#{post.id}" do
            = image_tag post.image.url, class: 'photo-list-img'